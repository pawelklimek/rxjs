export class Car {
  constructor(readonly productionHour: number,
              readonly name: string,
              readonly type: string,
              readonly price: number,
              readonly owner: string) {
  }
}
